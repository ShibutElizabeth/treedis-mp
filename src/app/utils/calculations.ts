// const getFarthestSweep = (allSweeps, graph) => {
//     const farthestSweep = allSweeps.reduce((prev, curr) => {
//         const prevDistance = graph.edges[`${currentSweep.sid}-${prev.sid}`]?.distance ?? Infinity;
//         const currDistance = graph.edges[`${currentSweep.sid}-${curr.sid}`]?.distance ?? Infinity;
//         return currDistance > prevDistance ? curr : prev;
//     });
// }


// console.log("Farthest Sweep:", farthestSweep);
